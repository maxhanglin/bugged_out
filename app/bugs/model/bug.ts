export class Bug {

    constructor(
        id: string, 
        title: string, 
        status: number, 
        severity: number, 
        description: number, 
        createdBy: string, 
        createdDate: number,
        updatedBy?: string,
        updatedDate?: number
    ) {}
}